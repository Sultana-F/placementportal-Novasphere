SYSTEM TESTING & UAT REPORT — Novasphere Placement Portal

Project: Novasphere Placement Portal
Version/Build: vX.Y (fill)
Date: (fill)

1. Test Objectives
- Validate correct functioning of all UI modules for Students, HODs, TPOs, and Principal/Admin.
- Validate integration across JWT auth, MySQL persistence, email notifications, ATS analyzer, file uploads, analytics, and status updates.
- Perform system testing for end-to-end workflows.
- Perform End-to-End testing using Postman.
- Perform UAT by exposing the system via ngrok for student testing.

2. Scope of Testing
Modules included:
- Authentication & Role-based dashboards (Student login, Admin/Staff login, logout).
- Student: registration, dashboard, apply job, ATS analyzer, profile update (avatar/resume upload), secondary data submission.
- HOD: students validation upload & confirm flow.
- TPO: post job drive, whatsapp blast request, job applicants view, update application status, download applicants (Excel/PDF), notice board CRUD.
- Principal/Admin: admin dashboard + analytics, staff management, application deletion.
- Chatbot endpoint.

3. Test Methodology
Manual component testing:
- Verified each route, validation messages, database changes, and UI rendering.

Integration testing:
- Verified interactions between JWT auth and DB updates.
- Verified job application persistence and dashboard aggregation.
- Verified TPO status update triggers email notification.
- Verified upload workflows (students Excel/CSV, profile photo, resume upload).
- Verified ATS analyzer pipeline: PDF extraction → AI JSON parsing → dashboard render.

System testing:
- Executed complete workflows across roles.

End-to-End testing (Postman):
- Tested API endpoint(s), verified status codes and response payloads.

UAT testing (ngrok):
- Exposed application publicly using ngrok and invited students to test.

4. Test Execution Summary
Manual Component Testing: PASS
Integration Testing: PASS
System Testing (E2E workflows): PASS
End-to-End Testing (Postman): PASS
UAT Testing (ngrok): PASS

5. Representative Test Cases (Pass Criteria)
- Student login: valid credentials redirect to student dashboard; invalid credentials show flash error.
- Admin/Staff login: correct dashboard routing based on role.
- Student registration: regno format/phone validation; duplicate email blocked; User + Student records created.
- HOD upload: validates required columns and row formats; duplicates detected; confirm inserts/updates ValidateStudent.
- TPO job posting & editing: deadline parsing; job creation; job updates persisted.
- Student apply: CGPA/backlog/active deadline checks; creates Application only once; redirects to external form link when present.
- ATS analyzer: PDF <5MB, job description length; extracted text analyzed; JSON parsed safely; result shown.
- Status updates: allowed transitions; student email notification triggered; DB status updated.
- Downloads: applicants Excel and PDF generated successfully.
- Chatbot: returns JSON reply; handles missing API key gracefully.

6. Defects/Issues Observed
- (Add any observed issues with screenshot/log references.)

7. Final Outcome
All critical flows executed successfully under manual, integration, system, Postman E2E, and UAT (ngrok) testing.
Final Result: PASS

8. Evidence to Attach
- Postman request/response screenshots.
- ngrok public URL screenshots.
- Email/Whapi/ATS logs screenshots where applicable.
- Test data samples: Excel/CSV and PDF resume used.

Sign-off:
Tested By: (Name)
Date: (fill)
Approved By: (Mentor/Guide)

